/** Explicit owner-run placeholder. Physical capture requires a browser/Electron runtime. */
console.log(
  "SKIPPED: run the browser capture harness with explicit Start/Stop; no microphone was accessed.",
);
