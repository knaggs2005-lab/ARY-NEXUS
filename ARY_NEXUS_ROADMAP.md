# Ary Nexus Roadmap

## Current Snapshot

- **Calls live setup — PARTIAL (September 10):** Existing Twilio provider enabled locally for the owner and one verified test destination. Authentication/US mobile Lookup passed; exact-approved native Ary call reached the provider but was rejected with HTTP 400. Twilio API and trial Voice Logs show no calls. Failed execution is audited; uncertain receipt and no-redial protection preserved. Trial Debugger is upgrade-gated; precise initiation error was discarded by the existing adapter. **37 focused tests passed.** No call delivery, paid upgrade, source/schema change or live acceptance DONE claim. [Report](CALLS_TEST_REPORT.md). Existing DONE statuses and NEXT 3 preserved.

- **Mac Desktop Bridge — ENABLED, bounded launch acceptance passed (September 10):** Existing owner/session-gated bridge enabled locally. Real installed-app scan, exact-approved Calculator launch, unchanged-receipt replay and persistent rejection audit passed. Installed launcher privacy descriptions updated using the existing packaging definitions, without application source/schema changes. **43 focused tests, 1,529 full tests / 88 files, typecheck, formatting and production build passed.** The macOS Documents consent prompt cleared; final native startup, authenticated UI, command-palette app discovery and a new approved Calculator launch passed. Native Automation/Reminders/other privacy and disruptive controls remain unverified. [Report](DESKTOP_BRIDGE_TEST_REPORT.md). Prior DONE statuses and NEXT 3 preserved.

- **Calendar approved-write test + personal Gmail setup — DONE (bounded September 10 acceptance):** Installed-app Calendar create → approved update → exact Google UI cleanup passed, with fresh empty-window readback and canonical audit receipts. Work Calendar now has approved-editing consent; personal Calendar remains read-only. Personal Gmail consent/search/read/attributed summary passed; no send scope or permanent email memory was added. Reused existing server-side Google client/vault/action systems, adding only local Gmail configuration, one callback and Gmail API enablement. **1,529 tests / 88 files, typecheck, formatting and production build passed.** Broader Calendar conflict/replay/voice and Gmail evidence/send/retention gates remain. See [Calendar report](CALENDAR_TEST_REPORT.md) and [Gmail report](GMAIL_TEST_REPORT.md). No application source/schema or NEXT 3 change.

- **Calendar chat across connected accounts — DONE (bounded read-only acceptance) (September 10):** Existing Calendar conversation bridge now reads all connected primary calendars by default, supports personal/work/exact-email and exact-domain targeting, and preserves per-account action permission/audit/idempotency. Responses label sources, merge chronology and overlaps, and disclose partial failures. Existing event-focus navigation carries the connection ID. **1,529 tests / 88 files, typecheck, formatting and production build passed**. Live dual-account and work-only chat plus source-event focus passed; final verification is recorded in [Calendar report](docs/calendar-multi-account.md). No scopes, schemas, providers, unrelated UI or NEXT 3 changes.

- **Personal + work Calendar accounts — DONE (bounded dual-account read-only acceptance, September 10):** Existing OAuth/provider/vault/action path extended in place to preserve up to eight accounts, select exact account for reads/recommendations, keep write consent and approvals account-bound, and disconnect one account independently. Legacy single-account records require no migration; Gmail is unchanged. **1,516 tests / 87 files, typecheck, formatting, production build and 17 isolated browser checks passed.** Installed-app personal OAuth and a real audited Calendar read succeeded; Clevaryn work read-only OAuth also succeeded after adding the work tester; both accounts remain connected. Initial work read was blocked by a cancelled Workspace subscription. After the owner resolved that blocker, fresh audited reads passed for both accounts: work returned zero events, personal one event, both complete for the seven-day window. No billing changes were made by Ary. The Calendar screen remains selected-account; the later chat extension below the report adds combined reads and conservative account targeting. [Report](docs/calendar-multi-account.md). Prior DONE statuses and NEXT 3 remain unchanged.

- **Learning-source onboarding — PARTIAL (September 10):** Existing conversation extraction/reflection retained. The original Clevaryn/Wag Trails source job completed after a bounded extraction-only timeout increase from 20 to 60 seconds; reviewed Wag Trails supersession/evidence and separate-conversation semantic + entity recall passed in the installed Supabase-backed app. 81 relevant regression tests passed. Restored an HTTP/2 transport failure by restarting the local server; misleading auth/profile error messages remain unresolved. Both Calendar accounts completed read-only OAuth; fresh personal and work reads both passed after the owner resolved the Workspace subscription blocker; Gmail was disconnected at that checkpoint; the later bounded setup above connected personal Gmail read-only. No background file/camera learning is enabled. No application source, schema, provider identity or permission policy changed. See [source audit](docs/LEARNING_SOURCE_AUDIT.md). Existing DONE statuses and NEXT 3 remain unchanged.

- **Hermes subordinate worker — DONE (bounded live advisory acceptance, September 10):** Separate pinned, restricted VPS worker now serves authenticated allowlisted TLS with owner-approved server-side OpenAI credentials and `gpt-5.6-sol` / `openai-api`. The real fixed diagnostic passed existing mandatory approval → action → result review → audit/outcome; isolated approved-action replay and reviewed episodic-memory replay passed, with cleanup verified (**4,739 ms final total, one submission, 10 actions/outcomes; local test embeddings only**). The physical signed-in Ary app received a real Supabase-backed diagnostic and accepted result review. Fresh **1,508 tests / 87 files (45 Hermes), typecheck, formatting and production build passed**. Seven public authentication/route checks and independent zero-tool inspection passed. Live cancellation passed (1,761 ms confirmed; audit/replay/cleanup verified). Idle restart retained key/run identity/completed status and canonical GET returned the original result (223 ms corrected local check); no data loss was observed. Terminal-replay hydration and final regression checks passed; temporary credential-transfer artifacts were removed while protected persistent configuration remains; forced in-flight restart is an untested limitation rather than an added acceptance gate; no full egress sandbox, cost ingestion or background watchdog is claimed. Existing Managed Hermes, schemas, hosted event-journal gate, prior DONE statuses and NEXT 3 remain unchanged. [Current report](docs/HERMES_TEST_REPORT.md#latest-verified-result--september-10).

- **Historical checkpoint — Hermes restricted VPS runtime — LOCAL ACCEPTANCE PASSED (September 10):** Official pinned non-root/read-only worker now passes real loopback authentication, disabled inventory, denied-route and durable failed-job replay checks, including container replacement. Additive `deploy/hermes/` package: 10 bootstrap tests (normal/optimized) and 25 YAML/security checks pass. Inference remains explicitly disabled; no existing model key was transferred and no public worker route was enabled. Model-credential authorization, successful inference and Ary end-to-end acceptance remain pending. This does not mark the integration DONE; prior DONE statuses and NEXT 3 remain unchanged. See [runtime acceptance](docs/HERMES_TEST_REPORT.md#restricted-vps-runtime-acceptance--september-10).

- **Historical checkpoint — Hermes restart compatibility (September 10):** Extended the existing provider to normalize upstream `interrupted` runs as failed with explicit unknown-completion evidence and no automatic resubmission. **38 worker tests, 1,501 total tests / 87 files, typecheck, formatting and production build passed.** The purchased VPS is running; restricted Hermes runtime, credentials and live diagnostic/restart acceptance remain pending. See [test report](docs/HERMES_TEST_REPORT.md#restart-compatibility-follow-up). Prior DONE statuses and NEXT 3 remain unchanged.

- **Historical checkpoint — Hermes live-setup follow-up (September 10):** Managed instance is running, but its public WebUI returned `not found` for capability discovery. Hostinger support returned a human-reviewed answer (Matheus, 13:03 Pacific) that the managed plan does not support the required external API routing; it requires a self-managed deployment. The user-approved 12-month KVM 1 purchase subsequently succeeded ($83.88; renewal $12.99/month), and VPS inventory confirms Running through 2027-09-10. Only the base Docker/Traefik server is running; restricted Hermes deployment and live acceptance remain outstanding. Existing Managed Hermes and worker credentials remain untouched. No live worker job ran. See [follow-up evidence](docs/HERMES_TEST_REPORT.md#managed-instance-follow-up--september-10). Existing acceptance statuses and NEXT 3 remain unchanged.

- **Historical checkpoint — Hermes subordinate cloud worker — IMPLEMENTED, LIVE ACCEPTANCE BLOCKED (September 10):** [architecture and secure setup](docs/hermes-worker.md), [test report](docs/HERMES_TEST_REPORT.md). Provider-agnostic boundary, existing owner messages/actions/outcomes, mandatory submission and proposal review, bounded keyed recovery, development diagnostics and explicit reviewed episodic memory. Actual local probe found no Hermes endpoint/key/restricted-worker confirmation; no remote job was sent and no live success is claimed. **1,498 tests / 87 files, final 35 worker tests, typecheck, formatting, production build and 12 isolated browser checks passed; cleanup verified.** No schema, deployment, credential, existing model/brain/graph change. Prior DONE statuses and NEXT 3 remain; configuration/live acceptance is this user-requested milestone's outstanding gate, not automatic permission to start another milestone.

- **Cohesion pass — IMPLEMENTED; complete production acceptance remains PARTIAL (September 10):** [factual report](docs/nexus-cohesion.md), [current implementation map](docs/nexus-current-state.md). No new major feature. Shared materials/loading, deferred screens, production mock-tool exclusion, truthful capability metadata, supported mobile command destinations and dead UI removal. **1,463 tests / 86 files, 12 Python tests, typecheck, formatting and production build passed.** Mobile (18), Communications (16) and Outcomes (12) isolated browser checks passed. Full desktop sweep remained incomplete after harness fixes and a final automation timeout; no clean pixel-regression pass claimed. Local process restart/transaction/security cases passed; npm production advisory snapshot reported zero known vulnerabilities. Hosted/native/mobile/provider gates remain explicit. No schema/dependency/configuration/credential/provider changes, no external outreach, no next milestone started. See report for measured performance and exact files.

### Historical milestone evidence

The dated entries below preserve what was verified at each milestone. They do not override the current cohesion report or turn fixture acceptance into live/deployed acceptance.

- **ARY Mobile Companion — IMPLEMENTED (web companion, September 10; physical-phone/deployment acceptance pending):** [native-versus-web evaluation, architecture, exact changes and tests](docs/nexus-mobile-companion.md). Dedicated `/mobile` presentation with voice-first entry, missions, reviewed quick capture, optional coarse one-shot location, camera/image input, bounded Nexus/memory/device surfaces and foreground approvals/activity. Reuses existing Brain/voice handlers, authentication, ToolRegistry, permissions, source capture, action/outcome/idempotency and events. Scoped install assets and network-only offline fallback; no private cache or offline action queue. **1,460 tests / 86 files**, typecheck, formatting and production build passed. Initial browser acceptance passed; expanded final acceptance verified core flows but stopped at a harness manifest connection reset (details in report). Background push and secure remote device execution are explicitly not enabled; no physical-phone acceptance or public deployment claimed. No schema/package/provider/credential change. Prior DONE statuses, hosted/native gates and NEXT 3 remain unchanged; no next milestone started.

- **ARY Communications — IMPLEMENTED (bounded preparation/debrief, September 10; conversational telephony pending):** [audit, adapters, LiveKit SIP evaluation and verification](docs/nexus-communications.md). Extends existing Calls/Gmail/Hub with canonical contact plans, channel preparation adapters, exact source-pinned requests and bounded communication debriefs proposing separately reviewed episodic evidence/follow-up tasks. Existing ToolRegistry, COMMUNICATE approvals, call safety, email storage, memory, task transactions, audit/outcomes and idempotency remain authoritative. Messaging and two-way calls explicitly report unavailable; no external outreach was sent. Browser acceptance passed 16 checks, including real isolated memory/task approval/replay; one synthetic live-model extraction passed in 3.695 seconds end-to-end with linked telemetry. **1,435 tests / 85 files**, typecheck, formatting and isolated production build passed; the report records one prior intermittent unchanged event-ordering assertion. No package/schema/credential/provider-setting change. Prior DONE statuses, hosted/native gates and NEXT 3 preserved; no next milestone started.

- **Premiere professional workflow — IMPLEMENTED (September 9–10; native IP-10 acceptance pending):** [audit, evaluation, exact changes and tests](docs/nexus-premiere-professional.md). Extends existing UXP adapter/ToolRegistry with source-linked timeline inspection/search, capability-aware native clip state/non-ripple removal and read-back, plus pinned-source FFmpeg/PCM analysis and approved transcription feeding the existing edit planner. Existing import/sequence/selects/export/receipts/permissions remain. Creative shows source evidence, findings and real action activity. **1,404 tests / 84 files**, unchanged QACutter **25 tests**, typecheck, formatting, production build and **17 browser checks** passed. Real synthetic FFmpeg and OpenAI STT approval/evidence/outcome/replay acceptance passed (2.329 s, one sample). FFmpeg installed; no npm/schema/production flag/key change. Transcript timing is excerpt-level, not word-aligned. No real Adobe project was edited or exported; native handshake/Undo/linked-clip/export verification remains IP-10. Prior DONE statuses and NEXT 3 preserved; no next milestone started.

- **Nexus ModelRouter — DONE (bounded implementation, September 9; additional vendor/local runtime acceptance pending):** [audit, configuration, exact changes and verification](docs/nexus-model-router.md). Extends existing provider interfaces with task/capability/privacy/context/cost/latency/availability routing, bounded cancellation-aware fallback, native Anthropic and compatible Gemini/llama.cpp/MLX transports. Systems responses show routing evidence; Ambient stays provider-neutral. Query embedding failure retains lexical/graph retrieval without changing stored embedding spaces. Total model outage yields explicit evidence-only responses; extraction/planning remain failed/retryable. **1,384 tests / 83 files**, typecheck, formatting, isolated production build and nine disposable browser checks passed. One live configured OpenAI gpt-5.6-sol call passed in 3.363 seconds (241 input / 7 output tokens; recorded estimate $0.001104). No extra provider, model setting, package, migration, production fixture or permission change. Local inference requires an explicitly configured server; hosted database outages still require retry. Existing DONE statuses, native/hosted gates and NEXT 3 remain; no next milestone started.

- **Nexus Outcome Engine — DONE (bounded implementation, September 9):** [audit, contracts, exact files and verification](docs/nexus-outcome-engine.md). Extends canonical outcomes/version history, Mission/Skill receipts and Economics with approved append-only assessments, corrections, metrics/lessons and source-linked people/company/project/strategy/agent/Skill connections. Transparent repeated-evidence proposals deduplicate retries and related mission/conversation runs; acceptance/rejection/withdrawal remain behind exact approval, action audit, idempotency and transactional rollback. Changed provenance deactivates advice. Acceptance is advisory and never edits core instructions, Skills, permissions or memory. Lazy **WORLD → Economics → Outcome learning** provides visual comparisons, receipts and reversible history. **1,347 tests / 81 files**, including 25 new service and 3 PostgreSQL cases, typecheck, formatting, isolated production build and **12 browser checks** passed. Browser acceptance included a real local task and audited mock failures; fixtures/server/browser cleaned up. No new schema/dependency, hosted/live setting, model call or external effect. Recent-outcome UI is capped at 200; legacy owner-scoped list reads remain. Rule v1 is a reviewable experiment suggestion, not causal inference or automatic behavior rollout. Previous DONE statuses, hosted/native gates and NEXT 3 are unchanged; no next milestone started.

- **Nexus Skills + Automations — DONE (bounded implementation, September 9):** [audit, contracts, exact changes and acceptance](docs/nexus-skills.md). Replaces the placeholder destinations with a lazy-loaded XYFlow workshop, immutable reviewed Skill versions, typed inputs/output bindings, conditions, bounded loops and pinned subskills. Six examples, shared conversation/planner proposals and manual/interval triggers reuse the existing MissionEngine, shared memory, ToolRegistry, permissions, exact approvals, audit, idempotency, outcomes and Nexus events. Triggers create pinned Mission drafts only; no silent permission grants or automatic execution. **1,319 tests / 79 files**, typecheck, formatting, isolated production build and **14 browser checks** passed, including real local task creation through separate approval and reviewed trigger delivery. The final suite retained original timeouts with two workers. No dependency/schema migration, production fixture, external effect, model change or live enable flag. Live generation/hosted worker acceptance remain separate; unresolved subskill inputs, arbitrary loops and cron/webhook triggers are outside this bounded version. Existing DONE statuses, hosted/native gates and NEXT 3 remain unchanged; no next milestone started.

- **Nexus Physical World — IMPLEMENTED (September 9; real hardware acceptance pending):** [audit, provider evaluation, exact changes, configuration and tests](docs/nexus-physical-world.md). Extends existing StudioConfig/StudioAdapter/StudioTool with DeviceRegistry, studio/home/office/future locations, positioned devices, timestamped telemetry, bounded read-only recovery and evidence-based readiness. Preserves Amaran; adds a restricted environment-configured Home Assistant light adapter. MQTT/ESPHome can remain behind HA; Node-RED is deferred. Existing MissionEngine executes the Podcast preparation spec through exact equipment approvals and fresh read-back; Nexus projects saved location/device references and real device events. **1,293 tests / 78 files at original timeouts**, typecheck, formatting, isolated production build and **14 browser checks** passed. Earlier host-load timing failures were cleared by the final full pass without weakening tests. No package, schema, live credentials/policies or enable-flag changes; no real hardware contacted. Camera/audio/other model-specific adapters and physical Podcast acceptance remain pending. All earlier DONE statuses, hosted/native gates and NEXT 3 are preserved; no next milestone started.

- **Persistent conversational interface + Ambient voice — IMPLEMENTED (September 9; physical live acceptance pending):** [audit, provider evaluation, exact changes and tests](docs/nexus-conversational-interface.md). Extends the existing voice/Brain path with AudioWorklet capture, replaceable local VAD, automatic final turns, barge-in, source-safe cancellation, wake-word architecture (disabled), opt-in background sessions, shared visual presence and Ambient conversation. Existing `mission.create` is now reachable from the shared conversation router; tasks/missions/memory retain current permissions, approvals, evidence, audit/outcomes and idempotency. **1,272 tests / 77 files**, typecheck, formatting and isolated production build passed; normal/reduced-motion browser acceptance used synthetic speech endpoints with real local Brain/memory/task/mission/approval records. Live synthetic OpenAI speech → extraction → paraphrased recall also passed. Measured synthetic barge-in ~212–230 ms; live STT 2.457 s, TTS generation 2.527 s, Brain first text 5.442 s (single samples, not physical speech-to-speech benchmarks). Retains ended-turn STT and sentence-buffered TTS; no WebRTC streaming transport, neural VAD model or wake model installed. Physical mic/echo, installed background/lock behavior and first-audible latency remain unaccepted. No dependency, schema, credential or model-setting changes. Existing DONE statuses, hosted/native gates and NEXT 3 remain; no next milestone started.

- **Controlled digital eyes and hands — DONE (bounded implementation, September 9; production enablement pending):** [audit, setup, exact files and acceptance](docs/nexus-digital-control.md). Adds isolated Playwright browser control, fixed native AX/window/keyboard/mouse control, transfer-folder documents and source-scoped one-frame visual fallback through existing ToolRegistry, permissions, exact approvals, audit/outcomes and idempotency. Tools → Computer & Browser shows real Nexus events; existing STOP CONTROL closes owned browsers and invalidates native targets. **1,251 tests / 74 files**, typecheck, formatting, isolated production build and normal/reduced-motion UI checks passed. Real Chromium form/file actions, actual native field/button/keyboard changes, and a live OpenAI synthetic-image → guarded native click were verified; fixtures were cleaned up. Existing dependency versions and the desktop's dependency symlink outside iCloud were preserved. No production flags, user policies, hosted migrations, brain/memory/graph or provider selection were changed. Owner app/origin configuration and installed-host privacy grants remain explicit; earlier hosted gates and NEXT 3 are unchanged. No subsequent milestone started.

- **Nexus Permission Engine — DONE (bounded implementation, September 9; hosted migration 017 pending):** [audit, contracts, exact files and acceptance](docs/nexus-permissions.md). Extends existing numeric policies with ten capability classes, owner-scoped agent rules and allow/ask/deny behavior; retains exact one-use approvals, capability ceilings, audit/outcomes and idempotency. Shared explanatory approvals and persistent emergency stop reuse existing UI, provider cancellation and durable mission pause records. Reset does not automatically resume missions. **1,228 tests / 73 files**, typecheck, formatting, isolated production build and normal/reduced-motion browser acceptance passed, including an approved real internal task and persistent-stop recovery. No new executor, external connection, live user policy or production fixture. Additive migration 017 is rerunnable and PostgreSQL-tested but not applied to hosted Supabase; earlier hosted/native gates and NEXT 3 remain unchanged. No subsequent milestone started.

- **Desktop launch recovery — VERIFIED (September 9):** [repair and native acceptance](docs/desktop-launch-recovery.md). Restored locked dependencies outside iCloud, retained the old tree as a backup, and selected Webpack for the existing launcher/dev/build commands. Seven launcher tests, formatting and the actual repository production build passed. Native Restart reopened the signed-in workspace, restored the saved real-provider conversation and returned to Ready. Existing event-storage warnings and hosted/native gates remain separate; NEXT 3 unchanged.

- **Nexus ToolRegistry — DONE (bounded implementation, September 9):** [audit, configuration, exact changes and acceptance](docs/nexus-tools.md). Extended the existing registry with normalized capabilities/authentication/location/availability, semantic + lexical discovery and an evidence-led Tools inspector. Official MCP client supports stdio and Streamable HTTP behind existing exact approvals, owner scope, audit/outcomes and execution keys; target/schema drift invalidates requests. MCP remains disabled by default; external server acceptance requires owner configuration. **1,185 tests / 71 files**, including 29 new cases, typecheck, formatting, production build and normal/reduced-motion isolated browser acceptance passed. No schema migration, provider selection, graph/memory redesign or live external effect. Existing native/hosted gates and NEXT 3 remain unchanged; no next milestone started.

- **MEMORY + WORLD visual intelligence — DONE (bounded, September 9):** [audit, exact changes and acceptance](docs/nexus-memory-world.md). Both destinations now open perspectives on the existing Nexus Canvas2D/vgpu map with source-linked entity → mission → outcome exploration, learned-memory inspection, episodic timelines, recorded changes/supersession, confidence/provenance and existing hybrid semantic retrieval. Original libraries/editors, storage, actions/permissions, providers and graph logic remain authoritative. **1,156 tests / 70 files**, typecheck, formatting, production build and isolated normal/reduced-motion browser acceptance passed, including a 900px desktop view. No new migration, dependency, external tool or production fixture. Context windows are capped; legacy owner-scoped list reads and previous hosted/native/live gates remain. NEXT 3 unchanged; no next milestone started.

- **Next-generation Nexus Memory — DONE (bounded implementation, September 9; hosted migration 016 pending):** [audit, contracts, exact changes and acceptance](docs/nexus-memory.md). Six orthogonal classes preserve existing IDs/types/evidence/embeddings; approved in-place classification, conversation-scoped working expiry, reviewed source-pinned consolidation, deterministic retrieval explanations, constrained transactional record deletion and separately permissioned/versioned Knowledge. Existing RRF/entity/temporal/Reflection/agent/action systems remain authoritative. **1,130 tests / 69 files**, standalone typecheck, formatting, production build, all-16-migration audit and normal/reduced-motion isolated browser acceptance passed. Knowledge/deletion and pre-limit SQL scope filtering require additive migration 016; older hosted learned retrieval retains v4 plus service filtering. No production data, hosted settings, provider selection, dependency or graph change. Prior 014/015/native/live gaps and NEXT 3 remain unchanged; no next milestone started.

**Canonical status and work-order document. Updated September 8, 2026; Ary Multi-Tool Orchestration v1 preserves all completed foundations and the September 7 recovery evidence.** Read this file before interpreting an old feature prompt. Inspect the relevant current implementation again before editing it. Historical `*_TEST_REPORT.md` files are evidence records, not competing roadmaps; their old “next milestone” recommendations are superseded by this file.

- **Architecture:** Next.js UI → existing catch-all HTTP API → domain/services → typed Repository → Supabase/PostgreSQL. `AryBrainService` composes existing intent/entity/retrieval/reasoning/extraction services. External and internal tools use `ToolRegistry` → `ActionRequestService` → `ActionService`/`PermissionService`.
- **Stack:** Next.js 16.3.4, React 19, TypeScript, PostgreSQL/Supabase, pgvector, Zod; Electron 44.2.0 desktop development launcher; vgpu 0.4.0 visual effects; MediaPipe hand detection. XYFlow 12.11.6 is installed for Mission Control and the bounded Agent Runtime family view; no Three.js, GSAP, Rive or separate agent-framework runtime. Reference repositories are reference material, not merged applications.
- **Active configuration:** local `ARY_STORAGE=supabase`, reasoning/embeddings `openai`; defaults are Responses API `gpt-5.6-sol` and `text-embedding-3-large` at **384 dimensions**, version `openai-te3-large-384-content-summary-v1`. OpenAI and Supabase credentials are present; values were not printed or copied. Mock/local/compatible providers remain explicit alternatives. Speech defaults are `gpt-4o-mini-transcribe` and `gpt-4o-mini-tts`; real synthetic-audio access passed September 7; physical/live-application acceptance remains incomplete.
- **Storage:** owner-scoped Postgres tables/RLS, vector/full-text RPCs, version/evidence tables, leased jobs and transactional batch RPC. `LocalRepository` is an explicit localhost-only development store, disabled in production. Google tokens/operation guards use a shared encrypted **single-host file vault**, not a second memory store.
- **Working foundations:** persistent memory/conversations, canonical identity/aliases, hybrid retrieval, reviewed temporal facts, graph query/rendering, actions/approvals/audit/idempotency, real task/project actions, Priority, advisory Board, base ROI, Finance statement visibility and spatial navigation. Live completion gaps are listed separately below.
- **Historical September 8 verification:** Ary Multi-Tool Orchestration v1 extends the existing coordinator: **907 tests / 57 files**, including 50 orchestration cases; **12 isolated browser checks**, typecheck, formatting and production build passed. Shared text/transcribed-voice controls and a Studio/Premiere/Calendar fixture scenario with an actual temporary internal task passed. See `MULTI_TOOL_ORCHESTRATION_TEST_REPORT.md`; historical real-model planning evidence remains in `ORCHESTRATOR_TEST_REPORT.md`. Cross-domain live acceptance stays IP-14 and native gaps stay IP-10 through IP-13. No external effects, schema/configuration changes or new identity. No separate lint command is configured.
- **Repository audit baseline (September 8, subsequent audit stage):** comprehensive component classifications and separate architectural/UI debt are in [docs/nexus-current-state.md](docs/nexus-current-state.md). Fresh **907 tests / 57 files**, **12 Python adapter tests**, production build and formatting passed. **Standalone typecheck failed** on byte-identical generated `.next/types/cache-life.d 2.ts` and `routes.d 2.ts` duplicates (TS6200/TS2300); build-internal TypeScript passed. No generated-file cleanup or application changes were made. This checkout has no commits or tracked files, so release provenance is not established. All existing milestone statuses and NEXT ordering are preserved.
- **Visual system (current):** Shared Nexus shell and Ambient/Systems modes, canonical dark material/font/focus tokens, Canvas2D/vgpu intelligence map, XYFlow mission/agent/Skill surfaces, and dedicated mobile presentation. Explicit orbital navigation remains a compatibility view. Domain-screen density and comprehensive visual acceptance remain partial; see the current cohesion report.
- **Next-generation design specification (September 8):** [docs/nexus-design-system.md](docs/nexus-design-system.md) defines Ambient/Systems modes, typography and material tokens, motion, operational/domain states, evidence and approval presentation, accessibility and incremental adoption. Stage 2 remains the presentation contract; its first shell implementation is now recorded below. Existing application source, milestone statuses and NEXT ordering remain unchanged.
- **Stage 3 application shell — DONE (bounded, September 8):** [implementation report](docs/nexus-shell-report.md). New default shell, eleven primary destinations, Ambient/Systems modes, shared presentation primitives, explicit orbit access, persistent command/status/attention and optional context. Existing screens and authority remain. **912 tests / 58 files**, typecheck, formatting and production build passed using identical source and freshly installed locked dependencies in an isolated directory after iCloud dependency-read failures; **13 isolated browser checks** include real task approval/execution/audit/outcome. Final responsive captures and reduced-motion check passed. Live native/provider gates and NEXT ordering are unchanged; no deployment or subsequent milestone started.
- **ARY visual presence v1 — DONE (bounded, September 8):** [implementation/evidence](docs/ary-presence-report.md). Shared event-driven folded light field with precise Brain phases, actual action/mission telemetry, Board stages, voice lifecycle and persisted approvals. Existing vgpu reused; static/reduced-motion fallback. **928 tests / 60 files**, typecheck, formatting, production build; native Metal 14-state readback and focused browser conversation→approved-task flow passed in WebGPU/static/reduced-motion modes. Full legacy shell/orbit browser run timed out and is not counted as passed. No schema/dependency/provider/graph changes; physical voice/Electron gates and NEXT order unchanged.
- **Normalized Nexus Event Bus v1 — DONE (bounded implementation, September 8; hosted activation pending):** [contract, audit, exact changes and evidence](docs/nexus-event-bus.md). Reuses the existing Brain/Board/action/voice telemetry and authoritative domain records. Fourteen event families, owner-scoped metadata journal, transactional source triggers, committed delivery cursors, authenticated/policy-checked resumable SSE and Systems-only raw Activity inspector. No new executor, provider, business store or graph. **969 tests / 62 files** (41 new event tests), standalone typecheck, repository formatting, production build and all-14-migration audit passed. Isolated browser chat → approval → real task/action/outcome → event inspector/reload passed; standard and reduced-motion captures checked. 349 source/test/script/migration/config files match the exact-locked-dependency verification checkout. **Migration 014 is included/rerunnable/tested but has not been applied to hosted Supabase.** Hosted delivery and scale acceptance remain pending; until installation the UI explicitly reports journal unavailability and retains the prior presence fallback. Previous DONE statuses, provider/native acceptance gaps and NEXT ordering are unchanged; no next milestone started.
- **Central NEXUS spatial map — DONE (bounded, September 9):** [renderer evaluation, exact changes, evidence and limits](docs/nexus-spatial-map.md). Existing Canvas2D/vgpu renderer extended with semantic regions, projected-depth orbit, zoom LOD, bounded source reads, filters/search, contextual evidence, canonical neighborhood drill-down and fresh event-linked paths. Original Entity Brain Graph retained. Real source records/roles/capabilities/inspection receipts are distinguished; empty automations/devices are not fabricated. **1,001 tests / 63 files** (32 new map tests), typecheck, repository formatting, production build and standard/reduced-motion isolated browser acceptance passed. One existing Communications test's ordering assumption was replaced by the same source-specific assertion. No new package, business schema, provider, executor or hosted setting. Map cap: 180 loaded/100 drawn nodes; existing Dashboard bootstrap still materializes legacy owner snapshots and needs later profiling. Hosted activity remains pending migration 014. Existing DONE statuses and NEXT ordering remain; no next milestone started.
- **Nexus Missions — DONE (bounded implementation, September 9; hosted activation pending):** [audit, Temporal evaluation, exact changes and acceptance](docs/nexus-missions.md). `MissionEngine` extends the existing coordinator/canonical plan messages with ten lifecycle states, leased checkpoints, branches, persisted waits/submissions, advisory shared-memory agent submissions, safe retries and a separately enabled per-user worker. Existing domain permission/approval/action/outcome authority remains. **1,040 tests / 65 files** (39 new mission cases), typecheck, formatting, production build and isolated browser approval → real internal task → verification → completion passed. A real process exits after task commit, waits for the actual lease expiry and recovers the same task; separate worker invocations complete an activated mission without a browser. **Migration 015 is additive and locally rerun/tested; hosted 014 + 015 and worker activation remain pending.** No Temporal SDK/service installed; provider-independent adapter boundary established. External uncertain effects wait for receipts, not blind retries; token refresh, process supervision and distributed live acceptance remain explicit deployment work. Existing DONE statuses and NEXT order are preserved; no subsequent milestone started.
- **Mission Control — DONE (bounded, September 9):** [audit, exact changes and acceptance](docs/nexus-mission-control.md). New read-only mission projection and pinned XYFlow execution graph within the existing Missions screen, with canonical objectives, agent/tool responsibility, waits/failures, exact approvals, receipt/output drill-down, cost evidence and event-driven refresh/poll fallback. Direct ID opening retains access beyond the 30-item recent list. **1,054 tests / 66 files** (14 new cases), typecheck, formatting and production build passed; isolated normal/reduced-motion browser approval → real task → verification → result inspection, reload, direct ID and responsive checks passed. Existing mission coordinator, actions/permissions, schemas, providers and Brain Graph/vgpu remain authoritative and unchanged. No new migration or hosted activation; earlier 014/015 and worker configuration remain pending. Arbitrary loops/sub-missions are not supported by the current engine and are not fabricated in the graph. Receipt rendering is capped at 200; server-side owner-history aggregation remains scale debt. All prior DONE statuses/NEXT ordering preserved; no subsequent milestone started.
- **Nexus Agent Runtime — DONE (bounded implementation, September 9; existing hosted activation gates retained):** [contract, exact files, validation and limits](docs/nexus-agent-runtime.md). Stable persistent/ephemeral worker profiles, functional specialization, inherited capability/tool/memory/permission scopes, server-owned model registry, atomic ancestry budget reservations, canonical mission assignments, delegation and durable subtree termination extend the existing coordinator. AGENTS opens the new inspector/XYFlow family view, with Daily Board preserved; central Nexus map and Mission Control reference real worker IDs. **1,088 tests / 67 files** (34 new agent cases), typecheck, formatting and production build passed. Normal and reduced-motion isolated browser tests verified profile → DRAFT mission → exact approval → real task → read-back/outcome/worker attribution → child node → subtree termination; graph framing and 900px layout passed, with no browser errors and fixture cleanup. No SQL migration, second brain/memory/action system, provider activation or external execution. Budget ceilings count runs/tool attempts/advisory calls/descendants/time; reported costs are not hard dollar caps. Child assignments require explicit activation; existing hosted 014/015 and worker configuration remain pending. Previous DONE statuses and NEXT ordering remain unchanged; no next milestone started.
- **Permissions:** levels 0–5, user/workspace/tool/action/product scoping, most-restrictive matching policy wins, exact-request approvals, audited retries and immutable history. Workspace currently means the existing `ary-nexus` scope, not a completed multi-workspace organization system. Calendar/Gmail writes and Finance imports retain mandatory approval ceilings even at level 5.

**Recovery audit update (September 7):** migration 011 is applied and rerun; both nullable double-precision cost columns are present with no defaults. Migration 013 removes unintended Supabase default UPDATE/DELETE grants on model telemetry and DELETE on Reflection history. **50/50 public table/function catalog objects match; all 13 versions are recorded once in protected migration history** as a verified manual baseline with source hashes. The original failed extraction job completed on attempt 3 through the installed app, adding one supporting quote to an existing memory; the workspace still has **8 memories, 0 failed extraction jobs and 0 expired extraction leases**. Following explicit user approval, **22/22 isolated Supabase memory/Economics checks passed** with real providers. Extraction, source evidence, reviewed supersession, paraphrased retrieval, cost revisions and replay safety passed. The temporary Auth account/profile and all **160 fixture rows** were deleted; independent checks found zero remaining fixtures across **27 user-owned tables**, zero auth identity/session and unchanged non-test records. **Recovery + Deployment Integrity is DONE**. No synthetic facts or costs remain in the working workspace. See [RECOVERY_DEPLOYMENT_TEST_REPORT.md](RECOVERY_DEPLOYMENT_TEST_REPORT.md).

Google Calendar client ID/secret are now installed in ignored local environment configuration; the existing configuration check passes and the shared vault encryption key is present. Gmail client ID/secret/redirect were subsequently configured locally on September 10; personal read-only consent/search/read/summary passed. The Mac being locked in earlier reports is historical, not the current explanation for incomplete setup. Recovery used the existing OpenAI embedding/extraction providers and wrote their action-linked cost telemetry. No mailbox access, camera recording or external execution was added.

The audit covered root structure, all 13 migration files, service entry points/core flows, API/UI wiring, provider configuration, real/mock registrations, desktop boundaries, graph/spatial/voice code, the test inventory and milestone reports. The recovery pass added normalized live catalog parity and a source-hash migration baseline. This is not a penetration-test certification or backup/restore certification.

## DONE

**DONE applies to the bounded milestone described below.** It does not certify unimplemented scale, live provider consent, public deployment, hardware quality or future extensions. Older rows retain their historical test counts; the current regression result is recorded in Current Snapshot. A hardware/provider or deployment gap has an explicit IN PROGRESS entry rather than being hidden behind a foundation's DONE label.

| Milestone                                                       | What exists and important files/services                                                                                                                                                                                                                                                                  | Verification / migration                                                                                                                                                                                                                                                             | Non-blocking limits for this scope                                                                                                                                                                                          |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Persistent memory, conversations and canonical storage          | `src/domain/models.ts`, `src/services/memory-service.ts`, `src/services/ary-brain-service.ts`, `src/infrastructure/repositories/{supabase,local}.ts`; users, memories, conversations/messages, entities, relationships, goals, decisions, tasks, actions/outcomes.                                        | `tests/services.test.ts`, `tests/knowledge.test.ts`, `tests/database.test.ts`; migrations 001–003; prior authenticated recall in `HYBRID_TEST_REPORT.md`.                                                                                                                            | Tenant ownership is established; shared commercial workspaces are not. Bounded recovery passed; host-lifetime/worker limits remain under ACTIVE RISKS.                                                                      |
| Source evidence, reviewed reconciliation and temporal facts     | `memory-reconciliation-service.ts`, `contradiction-guard.ts`, `memory-source.ts`, `knowledge-review.tsx`, `memory-sources.tsx`; exact quotes, conflicts, accepted supersession, immutable sources/version history, current vs historical reads.                                                           | `tests/knowledge.test.ts`, `tests/openai.test.ts`, SQL tests; migrations 002/009; real extraction/correction scenarios in `PHASE1_TEST_REPORT.md` and `OPENAI_TEST_REPORT.md`.                                                                                                       | Contradiction detection is bounded/model-dependent, not proof of truth. Explicit review remains required. Failed job recovered; controlled live correction/provenance acceptance passed in Recovery + Deployment Integrity. |
| Canonical entity resolution and aliases                         | `entity-resolution-service.ts`, `entity-service.ts`, `entity-resolution-debug.tsx`; canonical names first, aliases second, recorded affiliation disambiguation, confidence/reasons and stable IDs.                                                                                                        | `tests/entity-resolution.test.ts`, `tests/knowledge.test.ts`; migration 002; Ary Nexus/Nexus, Clevaryn/Wag Trails aliases, overlapping companies and ambiguous people covered.                                                                                                       | No similarity-only merges, automatic pronoun resolution or unrestricted semantic identity inference. Ambiguity is an expected result requiring clarification.                                                               |
| Hybrid semantic, lexical and graph retrieval                    | `memory-service.ts`, `hybrid-ranking.ts`, `graph-retrieval.ts`; pgvector + PostgreSQL full-text + direct links and 1–2 hops; eligibility filters and transparent reciprocal-rank fusion/debug traces.                                                                                                     | `tests/hybrid-retrieval.test.ts`, SQL/provider tests; migration 004; live paraphrase/lexical verification in `HYBRID_TEST_REPORT.md`.                                                                                                                                                | Importance/confidence are secondary. Graph-derived retrieval still loads tenant tables server-side; this is separate from the bounded graph-view RPC.                                                                       |
| Production reasoning/embedding interfaces and migration         | `domain/providers.ts`, `infrastructure/providers/{openai,compatible,local}.ts`, `embedding-record.ts`, `reembedding-service.ts`, `scripts/reindex.ts`; Responses streaming, version/model/hash metadata, idempotent re-embedding, telemetry and response badges.                                          | `tests/openai.test.ts`, `tests/providers.test.ts`; migration 003; real calls and the then-existing six-memory re-embedding/rerun verified in `OPENAI_TEST_REPORT.md`.                                                                                                                | Historical samples are not current service-level guarantees. Speech access is separate. Do not infer today’s embedding completeness from the earlier six-record snapshot.                                                   |
| Bounded graph data/query service                                | `domain/brain-graph.ts`, `graph-query-service.ts`, `repositories/local-graph.ts`, Supabase `query_brain_graph_v1`; node metadata, counts/blockers/goals, filters, search and depth 1/2.                                                                                                                   | `tests/brain-graph.test.ts`, migration 006; live 3-node/2-edge neighborhood/search and tenant checks in `GRAPH_TEST_REPORT.md`.                                                                                                                                                      | No production-scale SLO; per-node details are capped and historical filtering is not full audit-time graph reconstruction.                                                                                                  |
| Brain visual graph and optional vgpu enhancement                | `components/brain/brain-experience.tsx`, `brain-canvas.tsx`, `graph-engine.ts`, `brain-details.tsx`, `effects/*`; selection/fade, zoom/pan/search focus, incremental expansion, halos/path energy and graceful GPU failure.                                                                               | `tests/brain-ui.test.ts`, `tests/brain-effects.test.ts`; live graph, real Metal shader/pixel checks and device-loss fallback in `BRAIN_UI_TEST_REPORT.md` / `BRAIN_EFFECTS_TEST_REPORT.md`.                                                                                          | **Canvas2D remains the entity interaction renderer; XYFlow is installed for Mission Control, agents and Skills.** Current client cap: 240 nodes/900 edges. Preserve this working renderer.                                  |
| Reflection proposal/review foundation                           | `reflection-service.ts`, `domain/reflection.ts`, `reflection-panel.tsx`; asynchronous rule-based observations/proposals, evidence/versioned acceptance/rejection, stale-evidence rejection.                                                                                                               | `tests/reflection.test.ts`, SQL tests; migration 005; live job/panel evidence in `REFLECTION_TEST_REPORT.md`.                                                                                                                                                                        | Development-only review UI/API; no broad semantic self-improvement. No independent worker while the server is down. Independent operational draining remains deferred under ACTIVE RISKS.                                   |
| Permissions, registry, approvals, audits and retries            | `domain/{permissions,tool-registry}.ts`, `permission-service.ts`, `action-service.ts`, `action-request-service.ts`, `approval-dialog.tsx`, `action-center.tsx`, `permissions-panel.tsx`; levels 0–5, scoped restrictions, exact review/revision, one-use approval consumption, outcomes and keyed replay. | `tests/permissions.test.ts`, `tests/action-requests.test.ts`, SQL tests; migrations 007/010; live mock approval/replay in `ACTIONS_MILESTONE_TEST_REPORT.md`; index valid in this audit.                                                                                             | HTTP authentication/malformed-envelope rejection can precede action dispatch. Returned audits are durable data, not ephemeral context. This is not authorization for external autonomy.                                     |
| Existing tasks + real `create_task`                             | `tools/create-task.ts`, `domain/task-actions.ts`, `task-conversation-service.ts`, task proposal/receipt components; atomic task/action/outcome creation, project/entity/source linkage, grounded confirmation and idempotency.                                                                            | `tests/create-task.test.ts`, database rollback tests; no duplicate schema; real Wag Trails conversation → approval → task → confirmation in `CREATE_TASK_TEST_REPORT.md`.                                                                                                            | Narrow natural-language parser; tasks are not a generic autonomous planner. The separate extraction failure was recovered without changing task creation.                                                                   |
| Real `update_task`                                              | `tools/update-task.ts`, `task-actions.ts`, `task-change-review.tsx`, `task-update-card.tsx`; status/priority/title/description/due/link changes, before/after approval, optimistic concurrency and atomic audit/outcome commit.                                                                           | `tests/update-task.test.ts`, `tests/task-change-review.test.ts`, database tests; `UPDATE_TASK_TEST_REPORT.md`; existing schema/RPC.                                                                                                                                                  | Conflict means fresh review. No Calendar synchronization or second task store.                                                                                                                                              |
| Existing projects + real `update_project_status`                | `tools/update-project-status.ts`, `domain/project-actions.ts`, `project-state.tsx`, `project-change-review.tsx`; canonical entity-backed projects, status/priority/health/blockers/goals/notes and actual task rollups.                                                                                   | `tests/update-project.test.ts`, `tests/project-http.test.ts`, `tests/project-ui.test.ts`, database rollback tests; `UPDATE_PROJECT_TEST_REPORT.md`.                                                                                                                                  | No separate projects table or invented progress. Live preview/cancel preserved real state; rollback/failure writes use isolated stores.                                                                                     |
| Priority Intelligence v1                                        | `priority-service.ts`, `domain/priority.ts`, `priority/priority-view.tsx`, `use-rank-motion.ts`; transparent scores/evidence/what-if weights, deadline/dependency paths, unknown-impact disclosure and animated reorder.                                                                                  | `tests/priority.test.ts`, `tests/priority-http.test.ts`; 12 production-client checks and actual authenticated work ranking in `PRIORITY_TEST_REPORT.md`.                                                                                                                             | Weights are product judgments; no persistent ranking history or complete dependency-cycle analysis. Read-only planning, not execution.                                                                                      |
| Daily Board Meeting v1 — manually triggered advisory run        | `board-meeting-service.ts`, `domain/board.ts`, `board/board-view.tsx`; four specialist roles → Analyst → CEO, one bounded central-memory/goal snapshot, structured findings, one saved brief and audit.                                                                                                   | `tests/board.test.ts`, `tests/board-http.test.ts`; 12 browser checks and real six-role Supabase/OpenAI meeting in `BOARD_MEETING_TEST_REPORT.md`.                                                                                                                                    | No separate memories, autonomous workers, daily scheduler or automatic execution. Failed/partial reports remain visible. Reviewed conversion to actions is NEXT.                                                            |
| Base ROI accounting                                             | `domain/roi.ts`, `roi-service.ts`, `roi-dashboard.tsx`; evidenced estimates/actuals, time, influence/savings, confidence, attribution/version history and unknown-value handling.                                                                                                                         | `tests/roi.test.ts`, SQL tests; migration 008 and live zero-fabrication checks in `ROI_TEST_REPORT.md`.                                                                                                                                                                              | USD and attribution claims, not a bank ledger or causal proof. Compute/tool-cost extension passed isolated live acceptance; see Recovery + Deployment Integrity.                                                            |
| Ary Voice ports, streaming/presence and cancellation foundation | `domain/voice.ts`, `voice-service.ts`, `providers/openai-voice.ts`, `components/voice/*`; capture/transcript review, streamed text, sentence queue, mute/stop/interrupt, orb/meter and late-callback cleanup.                                                                                             | `tests/voice.test.ts`, `tests/voice-capture.test.ts`, `tests/voice-presence.test.ts`; real text streaming/extraction/continuity and browser controls in `VOICE_TEST_REPORT.md`.                                                                                                      | This DONE item is the foundation only. **Live speech/audio round trip is IN PROGRESS**; do not claim STT/TTS production success.                                                                                            |
| Spatial shell, motion and accessibility fallback                | `components/spatial/{spatial-shell,spatial-scene,module-orbit,module-card}.tsx`, `camera.ts`, `motion.ts`, `use-camera-system.ts`, `postprocessing.tsx`; 10 slots, interruptible focus sequence, settled idle, quality/HUD and mouse/keyboard/classic/2D paths.                                           | `tests/spatial-state.test.ts`, browser/production tests and authenticated navigation in `SPATIAL_SHELL_TEST_REPORT.md`; historical 60 FPS production sample, not a universal guarantee.                                                                                              | CSS depth/atmosphere, not physical volumetric simulation/XR. Existing workspace themes retained; new unified hero layout remains unbuilt.                                                                                   |
| Hand/gesture implementation and desktop capture-policy repair   | `spatial/gesture-engine.ts`, `hand-tracking.ts`, `hand-worker.ts`, `use-gestures.ts`, `desktop/security.cjs`, `desktop/main.cjs`; smoothing/dwell, target hit tests, one-frame backpressure, GPU→CPU recovery and camera cleanup.                                                                         | `tests/spatial-gestures.test.ts`, `tests/spatial-tracking.test.ts`, `tests/hand-worker.test.ts`, `tests/desktop.test.ts`; real MediaPipe inference with synthetic video in browser/Electron in `HAND_TRACKING_TEST_REPORT.md`.                                                       | **Physical-hand accuracy remains IN PROGRESS.** Navigation-only gestures; no approval clicks or business execution. Synthetic frames do not prove real hand accuracy.                                                       |
| Installed live-development desktop app                          | `desktop/{bootstrap,main,security,server-manager}.cjs`, `desktop/build.mjs`; installed Apple Silicon launcher, exact localhost boundary, owned process lifecycle and source hot updates.                                                                                                                  | `tests/desktop.test.ts`, HTTP health tests; installed authenticated app and source hot-update verification in `DESKTOP_TEST_REPORT.md`; installed Finance read rechecked during Finance milestone.                                                                                   | Depends on this Mac/repository/Node; ad-hoc signing only. No standalone notarized distribution or remote updater. Later hand-tracking report supersedes the old audio-only permission note.                                 |
| Finance v1 statement-visibility foundation                      | `domain/finance.ts`, `finance-service.ts`, `finance-conversation-service.ts`, `tools/finance-tools.ts`, `components/finance/*`; reviewed source imports, exact minor units, accounts/debt/holdings/bills, temporal corrections, currency-separated totals and “why” navigation.                           | `tests/finance.test.ts`, `tests/finance-database.test.ts`; migration 012 live; 14 fixture browser + 8 real browser/HTTP/disposable-persistence checks in `FINANCE_TEST_REPORT.md`; real app empty read passed.                                                                       | No bank/market connector. Real ledger currently empty. Latest-statement coverage only; data onboarding and reconciliation expansion are IN PROGRESS/NEXT, not a rebuild.                                                    |
| Recovery + Deployment Integrity                                 | Existing reconciliation entity-link dedupe and retry audit extended; original failed extraction recovered; migration 011 applied/rerun and 013 restricts unintended audit grants; migration baseline retained.                                                                                            | **580 tests / 42 files**, typecheck, formatting, build and installed-app recovery passed September 7; **22/22 real Supabase memory/Economics checks passed**, 160 isolated fixtures/account removed with independent cleanup verification. See `RECOVERY_DEPLOYMENT_TEST_REPORT.md`. | Small live sample; no independent job worker, full backup/restore certification or public deployment claim. No next milestone started.                                                                                      |

### Universal Ary Command Palette v1 — DONE (bounded implementation)

- Owner-selected September 8; existing navigation, aliases, workspace snapshot, ToolRegistry, action API, approvals, audit/outcomes, desktop security and STT reused. No additional backend/schema/provider.
- Body-portal ⌘K/Ctrl+K launcher; keyboard/mouse navigation, module/entity/project/task focus, catalog action form handoff, installed-app refresh, observed website metadata/direct URLs, strict prefix/initials/word-boundary/substring ranking and opt-in existing memory-search fallback. Shared voice destination resolver/dispatcher; ambiguous names require selection.
- Verified: 653 tests / 46 files, typecheck, format check and production build; 11 isolated real browser/HTTP/repository checks including approval → real task → audit/outcome → task recall. Installed app ⌘K and Wagtrails alias navigation passed. 10k commands averaged 0.88 ms search on this Mac; not a cross-device guarantee.
- Boundaries: installed app discovery/execution still requires IP-8 activation; native effects remain untested here. Physical microphone acceptance remains IP-3. Semantic fallback searches existing memory, never auto-executes a guessed command. Only loaded workspace records are indexed. See `COMMAND_PALETTE_TEST_REPORT.md`.

### Ary Communications Hub v1 — DONE (bounded implementation)

- Owner-selected September 8. Existing Gmail/Calls/Calendar action receipts, entities/current relationships, tasks, permissions and approvals supply one read-only source projection. No schema migration, copied email store, provider change or parallel pipeline.
- `domain/communications.ts`, `communications-service.ts`, and `components/communications/communications-hub.tsx` provide a source-tagged timeline, last observed contact, source-linked follow-ups, observed unanswered threads, pending approvals and explained review priorities by person/company/project. Existing Gmail source reader, Calls, Calendar, Activity and approval screens remain the action surfaces. No autonomous outreach or new memory writes.
- Verified: **714 tests / 49 files**, typecheck, format, production build; ten isolated browser/HTTP/task/approval checks passed and cleaned up. Installed authenticated Supabase hub shows the real empty state without inserting fixtures. See `COMMUNICATIONS_HUB_TEST_REPORT.md`.
- Limits: successful observed receipts only, not continuous sync. Reply obligation, human contact, business outcome and later Calendar changes remain unknown unless supported. Existing source-linked task creation is currently Calls. Messages/SMS are contract-only. Paginated UI still reads owner history through the existing materializing repository API. Live provider acceptance is not implied.

### Ary Edit Intelligence v1 — bounded advisory foundation

- Owner-selected September 8. Added a deterministic, English-oriented edit planner using timed transcript JSON/SRT, source hashes/quotes and optional local PCM/RMS audio measurements. Transparent question/answer, hook, gap/silence, repeated wording, selects, ranking, rough-cut and marker/export suggestions. No LLM/provider changes or claim of visual footage understanding.
- `edit.plan` and `edit.prepare_marker` extend the existing ToolRegistry and action/permission system. Plans and corrected versions remain in owner-scoped action/outcome history; no schema or permanent-memory changes. Creative reuses current glass/animation styles and existing Premiere approval controls.
- Reviewed source-to-sequence marker mapping produces a valid current-state Premiere request with source-action provenance. Trimmed ranges remain explicitly unsupported by the current execution adapter; whole-clip selects never substitutes for range edits. Export requires a real preset/destination and later approval. Analysis itself never executes Premiere.
- Verification and exact changes: `EDIT_INTELLIGENCE_TEST_REPORT.md`. Real Adobe acceptance remains IP-10; broad semantic editing, transcription/diarization and visual/audio-quality judgment are not part of this bounded DONE item.

## IN PROGRESS

IP-1 (controlled memory recovery acceptance) and IP-2 (Economics acceptance) closed on September 7, 2026 under **Recovery + Deployment Integrity**. Existing host-lifetime/background-worker limits remain documented under ACTIVE RISKS; they do not imply that the completed bounded acceptance milestone must be rebuilt. Remaining IP identifiers are retained for stable references.

### IP-3 — Voice v1 live audio acceptance

- **Working:** ports, providers, recording/transcript UI, text streaming, speech queue, interruption/cancellation tests and memory continuity from confirmed text.
- **Missing:** completed physical action/memory acceptance, measured first-audio/cancel latency, rapid-input and permission/autoplay recovery checks. Real synthetic provider evaluation and basic physical microphone/playback have passed as detailed below.
- **Latest September 7 result:** with explicit user approval, added only `gpt-4o-mini-tts` and `gpt-4o-mini-transcribe` to the existing project allowlist; original three models and all other settings preserved. Existing real-provider synthetic-audio evaluator passed: TTS 2,748 ms, STT 1,764 ms, first Brain delta 4,051 ms including retrieval, one extracted memory and successful paraphrased follow-up. Temporary local storage was removed. This is not physical/Supabase voice acceptance.
- **Additional probes:** direct reasoning-provider first token 7,344 ms; synthetic silence/noise/click returned empty text (silence needed one recheck after an intermittent allowed-model denial). Timing variation is not a diagnosed bottleneck. First audible output and physical cancel latency remain unmeasured.
- **Physical session progress:** user confirmed accurate installed-app microphone transcription of the Wag Trails question. Sending that exact preview produced one real OpenAI answer, completed extraction without new memories, and observed Speaking → idle. UI metrics: response 6,071 ms, model 2,732 ms, seven retrieved memories. Broader Ary Nexus graph context was also included; user confirmed clear, good-sounding playback. The corrected exact “about it” follow-up stayed on Wag Trails using conversation history, with zero detected entities/retrieved memories. Topical continuity passed; canonical pronoun resolution and evidenced decision retrieval remain unverified.
- **Observed name-recognition failure:** spoken Clevaryn became “cleveryn and instead,” resulting in zero entities/retrieved memories and failed topic switch. A small spelling-hint experiment did not improve it and leaked glossary words into silent-audio transcription; the experiment was fully reverted. Review/edit before Send remains the safe path. Physical stopping/no-overlap confirmation is separate.
- **Remaining gate:** interruption/rapid-input timing, voice-created approved task and live evidence/recall. User confirmed click-to-interrupt stops playback; proper-name recognition remains weak. Current press-to-record + transcript review supports click-to-interrupt, not hands-free barge-in while the mic is off.
- **Task-command repair:** observed “I create a task for me to review Wagtrails live tomorrow” fell through to ordinary reasoning and incorrectly denied task capability. Extended the existing task conversation adapter for polite requests and explicit clarification of unclear voice wording/unresolved project names. No automatic identity guessing or permission bypass. Eight new tests cover natural requests, non-command boundaries and shared Brain → approval → real local task/outcome → grounded recall. Installed-app retry could not be submitted because native control reported no available window; live task acceptance remains pending.
- **Owner spelling correction:** user confirmed **Wagtrails** is one word. Saved and verified that alias on the existing project through the installed app; prior guidance to insert a space was incorrect. Existing ID/links and legacy display name preserved. No code or schema change.
- **Voice continuity repair:** user reported a mid-response voice change. Installed app confirmed task clarification routes internally; the missing one-word project spelling was subsequently added as noted above. No completed task is claimed. Speech queue now combines already-available sentences into clips capped at 600 characters, reducing independent generation boundaries without waiting for future streamed text. The configured voice/provider stays unchanged. Audible improvement remains pending user verification; long streamed responses still span clips.
- **Transcription latency:** same STT endpoint now supports streamed partial preview after Finish recording, retaining final review/Send and audited completion. Eight real provider samples passed bounded content checks: short first-text mean 0.77s vs buffered 1.30s; longer sample benefit was small and variable. Separate real-provider/local-audit/client-parser flow passed at 1.16s first text / 1.19s complete with no messages or memories. Physical app timing is pending; this is not while-speaking dictation or a Wispr Flow benchmark.
- **Fresh gates:** 602/602 tests across 43 files, typecheck, formatting and build passed. Additive STT interface/adapter, voice service/stream transport, voice preview/timing, tests/evaluator and docs changed; no schemas, permissions, task or memory changes. Voice Live Acceptance remains IN PROGRESS. See the latest section in `VOICE_TEST_REPORT.md`.
- **Stage 2E-B1:** validated synthetic PCM16 frames now cross the realtime session boundary as exact `input_audio_buffer.append` payloads after provider readiness. No commit/response command, microphone wiring, Brain integration or network audio acceptance was added. Focused and full validation passed; physical realtime audio remains deferred.
- **Stage 2E-B2-A:** added an authenticated, same-origin, in-process development relay that reuses the canonical server-side realtime provider. It accepts bounded five-frame/100 ms batches, tracks only sanitized session metadata, enforces owner-scoped one-session concurrency, and closes on stop, provider failure/closure or expiry. No physical microphone wiring, playback, Brain, tools, memory or database table was added. Live relay acceptance remains blocked until the existing realtime handshake is corrected: the configured `gpt-realtime-2.1` run reached `session.created` but the provider rejected the existing `session.modalities` parameter as unknown.
- **Stage 2E-B2-A.1:** corrected current nested Realtime audio configuration and readiness through `session.updated`. Live handshake and bounded synthetic relay acceptance passed; physical microphone remains disconnected. Stage 2E-B2-B has not started.
- **Stage 2E-B2-B — implementation complete; owner acceptance pending:** extended the development microphone harness with explicit authenticated Realtime streaming using the existing capture provider and relay. Bounded 100 ms batches, 300 ms buffering, cleanup/failure metadata and no playback. Fresh live handshake and synthetic relay prerequisites passed; physical speech/VAD acceptance is NOT_RUN until observed. No later stage started.
- **Stage 2E-B2-B.1 — throughput/runtime correction implemented; fresh physical acceptance pending:** the owner's first physical attempt did forward five frames to OpenAI, then hit `BACKPRESSURE_LIMIT` with cleanup confirmed. Sequential 100 ms batches cannot sustain measured 178–260 ms requests. Revised client/server batches are 15 frames / 300 ms / 14,400 bytes with a 30-frame / 600 ms total bound including in-flight audio, whole-frame final flush and no audio retries. Normal npm dev/build/start and native child startup set `WS_NO_BUFFER_UTIL=1` before ws evaluation. Fresh **1,631 tests / 99 files**, typecheck and isolated production build passed; formatting retains only the four unrelated warnings. Standalone handshake, synthetic REAL_RELAY and authenticated browser relay on normal `npm run dev` passed (15 frames / 14,400 bytes, clean close). Fresh **PHYSICAL_REALTIME_MIC: NOT_RUN** until owner speech/VAD/Stop is observed; synthetic silence is not physical acceptance. `/api/events` storage 503 remains unrelated and untouched. [Detailed correction/evidence](docs/audits/GPT_LIVE_INTEGRATION_ARCHITECTURE.md#stage-2e-b2-b1--sustainable-relay-throughput-and-server-startup). No later voice stage started; NEXT 3 unchanged.
- **Files:** `VOICE_TEST_REPORT.md`, `scripts/evaluate-voice.ts`, `domain/voice.ts`, `providers/openai-voice.ts`, `components/voice/*`.
- **Completion:** owner-controlled access to the existing speech models, then run the existing evaluator and a real device check. Wake word/continuous streaming STT are deferred, not v1 blockers.

### IP-4 — Calendar v1 live connection and controlled external-action verification

- **Working:** Google adapter/OAuth/vault, read/conflict/entity context, recommended time blocks, mandatory-approved create/update, stale ETag protection, idempotent external-receipt recovery, integrated timeline; 32 Calendar tests and 13 fixture browser checks.
- **Current live status (September 10):** personal/work consent and reads passed; owner-approved work test event/create/update and exact cleanup passed. Live replay/denial, voice writes and fresh conflict preflight remain pending. Historical setup blockers below are superseded by the latest reports.
- **Blockers:** downloaded OAuth Web-client JSON validated for project `ary-nexus` and the exact existing localhost callback; client ID/secret installed in ignored `.env.local` with mode 0600. Existing configuration check returns true. Google confirms External/Testing with only the owner test user and Calendar API Enabled. Actual Calendar consent and live reads remain pending. Google still shows a branding-incomplete banner although required name/support/developer-contact fields are populated; investigate during consent. Installed app reported Failed to fetch before sign-in; local server HTTP check returns 200. The Mac then locked and automatic unlock failed, blocking interactive consent. No live Calendar calls/events. Audit also found missing write-time conflict rechecks/explicit conflict approvals, dropped location detail, narrow Chat intent coverage, and no combined project-priority ranking. These must be addressed before claiming the requested acceptance.
- **Files:** `domain/calendar.ts`, `calendar-service.ts`, `calendar-conversation-service.ts`, `infrastructure/calendar/*`, `server/calendar-oauth.ts`, `tools/calendar-tools.ts`, `components/calendar/*`, `CALENDAR_TEST_REPORT.md`.
- **Completion:** read-only consent/read first, then explicit approval of exact test writes. Primary calendar only; no invitations, recurring-series edits or autonomous scheduling.

**Current active milestone (owner request, September 8):** Desktop Bridge v1 supersedes Calendar as the active work order. Calendar setup and acceptance remain open and unchanged by the bridge. Voice is not retroactively DONE. No real Calendar event created or deleted.

### IP-5 — Gmail v1 live connection and evidence/send acceptance

- **Working:** bounded selected-thread search/read, summary, canonical links, local drafts, exact-quote reviewed evidence, mandatory-approved sending, encrypted operation guards and uncertain-send recovery; 37 Gmail tests and 14 fixture browser checks.
- **Missing:** live draft/evidence acceptance and a separately authorized controlled send/replay check. Define retention for email content already retained in action receipts. Personal consent/search/read/summary passed September 10; Gmail remains single-account and read-only.
- **Current status:** local configuration and real personal Google consent/search/read/model-summary are verified. Sending remains disabled; retention/evidence/send acceptance is still pending.
- **Files:** `gmail-service.ts`, `infrastructure/gmail/*`, `tools/gmail-tools.ts`, `components/gmail/*`, shared vault/approval infrastructure, `GMAIL_TEST_REPORT.md`.
- **Completion:** read consent and bounded source validation, then reviewed evidence and explicit exact-recipient send approval. No test email may be sent merely to complete this roadmap.

### IP-6 — Physical hand navigation/device acceptance

- **Working:** navigation engine, camera routing, synthetic browser/Electron inference, GPU/CPU timeout recovery and cleanup.
- **Missing:** actual installed-app permission prompt, one-hand targeting/pinch/swipe/focus measurements under representative lighting, false-trigger/cursor-drift observations and real camera indicator shutdown.
- **Blocker:** no documented real-hand recording/acceptance. Earlier locked-Mac condition is historical; current lack of measurements remains.
- **Files:** `HAND_TRACKING_TEST_REPORT.md`, `scripts/evaluate-desktop-tracking.cjs`, `components/spatial/*`, `desktop/security.cjs`.
- **Completion:** observe current behavior on real hardware, tune only measured gesture/lifecycle faults, preserve full mouse/keyboard access. No new gesture authority.

### IP-7 — Finance real-data onboarding and coverage completeness

- **Working:** statement-based v1 and live empty ledger; real local end-to-end approval/persistence/replay and SQL invariants verified.
- **Missing:** first user-provided real source, reconciliation of overlapping/nonoverlapping statement coverage, paginated reporting before large ingestion, and a clear account-identity/coverage workflow. None of these require another financial store.
- **Blocker:** no real source imported; v1 JSON is normalized manually, and latest-snapshot transactions cannot represent all historical windows automatically.
- **Files:** Finance files/report above, existing repository interfaces.
- **Completion:** validate an actual source through current approval/UI, then add narrowly specified coverage reconciliation with provenance and double-counting tests. No bank connector, prices or financial conclusions inferred from absent records.

### IP-8 — Desktop Bridge v1: enabled, app-launch acceptance passed; broader native acceptance pending

- **Existing systems reused:** installed Electron launcher, fixed loopback origin, authenticated ActionRequestService/ToolRegistry, scoped permissions, exact approvals, audit/outcomes, request-key replay and encrypted single-host vault primitive. No new schema or execution API.
- **Added:** fixed app/website/media/volume/clipboard/hide/quit/lock/display/Focus/Notes/Reminders tools; fresh app scan; `execFile` argument arrays and fixed JXA source; private owning-launcher proof; pinned authenticated owner; disabled environment gate; Settings section. Every effect and clipboard read requires approval even at level 5.
- **Verified:** bridge-specific injection, permission, HTTP approval, replay and uncertain-result recovery tests; real read-only native scan found 111 apps in 646 ms with isolated action/outcome/replay checks and fixture cleanup. Installed Settings rendering and real-account disabled denial/audit checked. Full final gate counts are in `DESKTOP_BRIDGE_TEST_REPORT.md`.
- **Current September 10 acceptance:** flag and owner pin enabled; real scan/approved Calculator launch/replay passed with owning-launcher proof. Final updated launcher starts successfully; command-palette launch and rejected-action audit passed. Other native controls/privacy prompts remain uncertified. Focus requires Ary Focus On/Off shortcuts. Clipboard/native content stays in audited records, not general semantic memory. See the latest Desktop Bridge report.
- **Completion:** explicitly enabled owner-bound session, harmless approved native action/replay, intended macOS privacy access, native IDs/state verified and disruptive controls tested only with the owner present. No Premiere/CAD, arbitrary shell, shutdown/restart/delete or force-kill verbs.

### IP-9 — Ary Calls v1: implemented; live provider acceptance pending

**Owner-selected active setup (September 10):** current adapter/guards audited; 37 phone tests passed. Local telephony configuration is absent and Twilio Console requires sign-in. Account/caller setup and an agreed test recipient remain pending. No calls, purchases, new credentials, enablement or source changes. See current CALLS_TEST_REPORT.md audit.

- Owner selected Calls after the command palette. Added `domain/phone.ts`, `services/phone-service.ts`, Twilio adapter, PhoneTool registration and Calls/review components. Existing registry/permissions/approvals/action receipts/outcomes/entities and real task creation are reused. No schema migration or second pipeline.
- Script-only outbound delivery; fixed automated identity, exact approval at levels 4/5, explicit user-intent input, US mobile/landline provider lookup, operator destination allowlist, 3/hour / 10/day / once per destination per day, one active/uncertain initiation. Persistent encrypted single-host guards prevent automatic redial after uncertainty and recover known provider receipts after database failure.
- Saved contacts use existing person/company `metadata.phone`; input number is pinned and rechecked. Status refresh/cancellation remain registered actions. Provider-supported/consented transcripts and summaries stay attributed to action/outcome history; follow-up uses `create_task` with source-action evidence. Twilio v1 recording/transcription is off; no recipient dialogue or permanent-memory ingestion.
- Automated: 690 tests / 48 files (37 Calls tests), typecheck, format and build gates; nine isolated browser/HTTP/action/task checks passed with a test-only telephony port. No real call was placed. Exact gates and limits are in `CALLS_TEST_REPORT.md`.
- Remaining: configure provider credentials/verified caller, owner and destination allowlist deliberately; approve one controlled call to an agreed recipient and verify receipt, actual script, status/cancel and cost. Provider choice remains swappable. No provider account creation, permission changes, purchasing, real dialing or background retries were performed. **Calls live acceptance is not DONE.**

### IP-10 — Ary Creative Premiere Pro v1: implemented; native acceptance pending

- Owner-selected September 8. Audited existing ToolRegistry, permissions, exact approvals, action/outcome history, encrypted single-host receipts, installed Premiere 26.3.2, UXP Developer Tools and the separate Clevaryn UXP editor. Reused the existing Ary pipeline and official UXP APIs. No schema migration, dependency, provider replacement or edits to Clevaryn's plugin.
- Added `domain/premiere.ts`, `infrastructure/premiere/bridge.ts`, `infrastructure/tools/premiere-tools.ts`, a dedicated `premiere-plugin` and scoped Creative panel. Twelve deterministic operations, observe/plan capabilities, state/ID/path validation, mandatory approval for every change, claim-once execution and immutable receipts. Unknown/partial native completion blocks further edits rather than automatically replaying.
- Verification: 40 new tests plus the existing 714 = **754/754 across 51 files**, with two workers to avoid an existing parallel PGlite initialization timeout. Typecheck, Prettier and production build pass. Eight disposable browser/HTTP/action checks pass; fake Adobe DOM tests cover all twelve API mappings. These are not real Adobe acceptance.
- Remaining: owner-selected disposable project/media/preset/export folders; explicit local flags/owner/token; load and connect the separate UXP panel; validate native handshake and each actual operation. Bridge remains disabled, `.env.local` untouched, no live project edited. See `PREMIERE_TEST_REPORT.md` and `premiere-plugin/README.md`. **Do not mark Premiere live acceptance DONE yet.**
- Limits: bounded inventories, structured JSON inputs, non-drop relative timecode, ordered source-media selects, export submission only, structural revision coverage, single-host transport journal and no atomic Adobe/Supabase transaction. No autonomous/destructive editing, new memory extraction or new natural-language command path.

### IP-11 — Ary Design Tool Framework: implemented; Cinema 4D native acceptance pending

- Owner-selected September 8. Audited the registry/pipeline, native boundaries and installed apps. Cinema 4D 2026.2 was found; no Blender/Fusion/FreeCAD install was found. Added one real SDK adapter implementation behind an application-neutral `DesignTool` interface; no other app targeted.
- `domain/design-tool.ts`, `infrastructure/design/bridge.ts`, `tools/design-tools.ts`, `design-plugin/*` and a scoped Creative Design panel. Existing permissions/approvals/outcomes/audit/idempotency remain authoritative. No schema, dependency or model/brain changes.
- Inspect and plan before changes; all mutations require approval. Fixed box primitives/local mm dimensions/display color, approved document open, new-version save/OBJ export, latest unchanged Ary undo and viewport preview. STEP, arbitrary scripts/macros, general material graphs and autonomous geometry editing are explicitly unsupported.
- Verification: 793 TypeScript-suite tests / 53 files, 12 Python SDK-port tests, typecheck/format/build and eight isolated browser checks passed. Browser fixture approved a 50→60 mm width change and verified result/approval/outcome links. No real geometry touched.
- Native gap: restricted c4dpy probe exited 139; unrestricted probe reached license-method selection. No account/method chosen and the owned probe was stopped. Feature flag remains disabled, local secrets unchanged, plugin not installed. Confirm licensing and disposable scope, then perform the native checklist. **Do not mark live Design acceptance DONE.** See `DESIGN_TOOL_TEST_REPORT.md`.

### IP-12 — Ary Studio Control v2: implemented foundation; physical acceptance pending

- User authorized a fresh studio implementation September 8 after confirming the earlier ARYN project is on another computer. The initial source blocker in `STUDIO_CONTROL_AUDIT.md` is superseded. Existing Nexus brain, shared storage, permissions, approvals, registry and action/outcome infrastructure remain authoritative.
- Added typed scene/device contracts, scene planning, dependency/readiness checks, serialized executor, encrypted per-step receipts and conservative uncertain-delivery guards. Three Studio tools, one Amaran Desktop OpenAPI adapter implementation, explicit scene conversation intent, Studio panel and Action history report. No new tables, migrations, execution API, dependency or separate assistant.
- Current verification: **826 tests / 54 files**, including 33 Studio tests, and eight fixture-only browser checks. Typecheck, formatting and production build passed; evidence is in `STUDIO_CONTROL_TEST_REPORT.md`. Tests cover permissions, exact approval, stale/tampered plans, source isolation, partial failures, dependencies, replay and database recovery after device effects.
- Physical gap: Amaran Desktop is not installed; no paired fixture IDs/key or hardware effects verified. Default scenes remain unconfigured, control disabled. Other device categories remain unsupported until specific adapters are available. Uncertain receipts require operator recovery; no automatic retry/guard deletion. **Do not mark Studio physical acceptance DONE.** Next Studio step is one controlled, approved Amaran-light test, then a model-specific camera/audio adapter.

### IP-13 — Ary Perception v1: implemented foundation; physical capture acceptance pending

- Owner-selected September 8. Audited existing providers, camera/voice lifecycle, desktop boundaries and action infrastructure before adding an independent `VisionProvider`/capture interface. Reuses ToolRegistry, exact approvals, permissions, outcomes, audit, idempotency and existing OpenAI transport/telemetry. No schema, model setting, permanent image store or second brain.
- New Perception panel handles uploaded screenshot/image, explicitly selected screen/window and one webcam/studio frame; inspect/verify/compare reports link to owned actions without changing their status. Separate one-use source and analysis approvals, RAM-only expiring buffers, visible activity and cleanup/cancellation. Existing hand tracking/voice streams are untouched.
- **857 tests / 56 files**, isolated synthetic-image browser acceptance, typecheck/format/build passed. One real `gpt-5.6-sol` comparison correctly detected an export dialog, not completed export, in 5,856 ms; replay caused no second model call. Fixture records/buffers cleaned. Details and exact files: `PERCEPTION_TEST_REPORT.md`.
- Physical gap: no user screen or camera captured. Electron native picker depends on supported macOS 15+ and a desktop restart; unsupported fallback denies. Studio cameras must be OS devices or uploaded frames. Source authenticity and device causality are not independently proven; provider retention settings still apply. Complete one supervised harmless capture/denial/cancel and real visible-state comparison before marking physical acceptance DONE.

### IP-14 — Ary Multi-Tool Orchestration v1: software verified; cross-domain live acceptance pending

- Audited and extended the existing coordinator in place. Ary remains the only identity; current navigation is **Execution Plans**. Shared brain/memory/entities, registry, actions, approvals, idempotency, outcome/verification infrastructure and existing plan IDs remain intact. No schema migration, new framework or new provider.
- Bounded dependency plans now support up to three independent whitelisted observation tools across different domains; mutations and same-domain reads remain serial. Added explained execution states/failures, exact related approval review, persistent pause/cancel, owner-reviewed revisions with fresh authorization, and shared text/transcribed-voice controls. Completed/uncertain effects retain evidence. Existing optional reviewed outcome memory remains central.
- **907 tests / 57 files**, including **50 coordinator tests**, and **12 isolated browser checks** passed. Typecheck, format and production build passed. Studio/Premiere/Calendar fixture scenario plus real temporary task creation/read-back/outcome-memory passed. Browser revision/approval/task/replay/evidence checks passed; fixtures removed. See `MULTI_TOOL_ORCHESTRATION_TEST_REPORT.md` for exact files and limits. Historical configured-model planning (6,866 ms, estimated $0.079016) remains in `ORCHESTRATOR_TEST_REPORT.md`; not re-measured this milestone.
- Live gap: the real Studio → Premiere → task → Calendar workflow still awaits configured hardware/native/Google connections, explicit concrete approvals and verified state. Physical voice acceptance remains its existing gate. The newer MissionEngine supplies an explicitly enabled worker; hosted activation remains pending. No automatic scope-changing re-plan or blind external retries. Group review is individual exact grants, not an atomic cross-tool transaction. **Do not mark cross-domain live acceptance DONE** until controlled real tests pass; no next milestone started.

## NEXT 3

The current owner-selected **Execution Plans cross-domain live acceptance (IP-14)** awaits configured, controlled domain tests. **Perception physical acceptance (IP-13)** awaits one supervised approved source test. **Studio Control physical acceptance (IP-12)** is pending actual devices and Amaran setup. **Design Framework native acceptance (IP-11)** remains pending Cinema 4D licensing and a disposable scene. Edit Intelligence is implemented; its Premiere native prerequisite remains IP-10. Complete these controlled native gates before considering this retained follow-on queue. Communications Hub implementation and isolated acceptance remain complete. No next milestone or outreach starts automatically.

1. **Ary Calls controlled live acceptance.** Confirm the first provider and eligible caller number, configure owner/destination limits, then approve a specific harmless call to an agreed recipient. Verify actual script, cancellation/status and provider receipt/cost; do not treat fixture tests as telephony acceptance.
2. **Desktop Bridge controlled live acceptance.** Preserve the disabled owner/session gates; verify the least disruptive approved native action first, then owner-supervised privacy/Notes/Reminders/media controls.
3. **Google Calendar v1 live acceptance.** Complete existing consent/read/approved event/update/replay/cleanup checks. Voice acceptance remains the next queued independent item below; no Gmail or autonomous external actions.

## NEXT 10

This is the single ordered queue after the current state. **The NEXT 3 order above takes precedence: after the current Premiere acceptance gate, Calls is followed by Desktop Bridge and Calendar. Voice remains queued. Gmail remains deferred.** Blocking credentials may allow the next independent item to proceed after an explicit scope decision; no silent roadmap reorder.

1. **Voice live acceptance** — finish the current provider/device round trip and interruption/continuity measurements.
2. **Calendar live acceptance** — finish existing OAuth/read/approved-create/update integration with real receipts.
3. **Gmail live acceptance and retention policy** — complete the present adapter's read/draft/evidence flow, then a separately approved controlled send. Reuse the shared credential/action architecture.
4. **Board/Priority → reviewed internal actions** — let the user select an evidenced finding, preview and submit it through existing create/update-task/project tools; preserve finding IDs, reject stale evidence and deduplicate repeated conversion. No automatic execution of a brief.
5. **Finance source onboarding and coverage reconciliation** — validate a real statement, prevent duplicated/omitted coverage, retain correction history and add bounded/paginated reads. A read-only source adapter is a later choice after this evidence is stable.
6. **Physical hand-tracking acceptance** — measurable real-device calibration, false-trigger reduction and accessibility/cleanup verification; no new action authority.
7. **Command palette live refinement** — foundation is DONE above. Validate physical spoken destinations and enabled app discovery with IP-3/IP-8; broaden beyond loaded records only if actual workspace scale requires it. Preserve existing routes, ToolRegistry and one dispatcher.
8. **Unified holographic workspace composition** — audit/reuse current tokens, panels, spring/ambient systems to add the requested center/left/right/lower-console layout to Home, Chat, Graph and Approvals. Keep all data/interaction services intact; implement only after operational blockers above are closed or independently deferred.
9. **First Clevaryn/Wag Trails read-only product adapter** — choose one real product/source contract, explicit narrow credentials/scope, canonical linking and source attribution; prove read usefulness before any approved writes. Existing seed entities are not product integrations.
10. **Operational rollout readiness** — validate retention/export/restore, managed vault/locking and usage limits before any public or multi-host release. Preserve existing providers and permissions; do not publish automatically.

## LATER

- Independent scheduled job/Board operation and proactive follow-up: reuse existing jobs, idempotency, shared evidence and cost controls; require restart/recovery and owner scheduling policy first.
- Multi-host/public production deployment: managed encrypted vault/locking, database-side aggregates, distributed usage limits, retention/export/deletion policy, stronger security/load testing and deployment automation. Local functionality is not proof of readiness.
- Notarized standalone desktop releases and signed remote updates: preserve exact-origin/process ownership; separate from the current source-live development launcher.
- Broader shared-workspace/commercial multi-tenant onboarding, billing and organization roles: keep existing owner isolation until membership/authorization/migration semantics are explicitly designed.
- Read-only Drive/document integration and additional third-party product adapters: require a demonstrated source need, existing approval/audit gates and selective memory evidence.
- Real `create_note`, generic message-draft or project-summary tools only when a concrete workflow requires them. Existing `mock.*` variants are simulated; reuse current memory/project services instead of adding stores.
- Investing analytics, historical market sources and **Kronos**: only after real Finance coverage/provenance is stable and explicit read-only interfaces/evaluation criteria exist. No claim of forecasting accuracy.
- Advanced financial execution/autonomous trading: not an authorized product commitment; requires a separate threat/risk model, explicit owner scope, approvals, reconciliation and recovery controls. None is supplied by Finance v1.
- Desktop bridge expansion and multi-display control: v1 is now implemented under IP-8, disabled pending native-write acceptance. Further capabilities remain deferred; no generic shell, shutdown/restart or file deletion.
- Native mobile/iPhone client: stable authenticated APIs, secure credential/session design and accessible non-spatial interactions first.
- Two-hand gestures, XR/multi-environment worlds and richer 3D: require measured benefit and real-device reliability; preserve static/2D mode.
- Wake-word/continuous listening or full streaming STT: explicit privacy/capture lifecycle and latency budget first; ordinary voice must work before this expansion.
- More autonomous agent orchestration: only after reviewed plan-to-action usefulness is proven, budgets/stop conditions are defined and all roles remain on central memory/permissions.

## DO NOT TOUCH YET

| Protected area                                                                    | Reason / prerequisite                                                                                                                                 |
| --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Money movement, payments, brokerage execution and autonomous trading              | Finance is source visibility only. No execution authorization, reconciliation guarantees or reviewed risk contract exists.                            |
| Autonomous sending, scheduling, purchases or contractual commitments              | Existing Gmail/Calendar writes require explicit approval; raising a generic policy to level 5 must not remove their ceiling.                          |
| Kronos                                                                            | Finance completeness and an evaluated read-only integration contract must precede it. No source imports constitute a market model.                    |
| Shutdown/restart, file deletion, arbitrary terminal/shell commands                | Desktop Bridge v1 does not expose these verbs. The launcher's narrow owned-server lifecycle must not become a user/agent command executor.            |
| Parallel memories, second Brain, replacement Postgres/provider/permissions stacks | Existing canonical data and interfaces work. Audit/reuse them; a reference repository or old prompt is not a reason to replace them.                  |
| Completed graph/retrieval architecture                                            | Canvas2D + vgpu and SQL/service graph interfaces are the reality. Do not install/replace with xyflow merely to make an old prompt's terminology true. |
| Automatic confirmed-fact rewrites, automatic email-to-memory ingestion            | Evidence, correction review and version history are required; generated interpretations are not confirmed user facts.                                 |
| Gesture-triggered approvals/high-risk actions                                     | Gesture confidence is heuristic. Keep navigation separate from authority.                                                                             |
| Public/multi-host rollout of the local token vault or demo identity               | File locks and shared demo identity are single-host development assumptions, not public deployment controls.                                          |

## ARCHITECTURE GUARDRAILS

1. Audit before modifying: read this roadmap, current files, migrations and tests; distinguish code, configured access and live acceptance.
2. Never duplicate an existing system. Old prompts are not a missing-feature inventory.
3. Prefer additive changes with the smallest reviewed scope.
4. Preserve stable interfaces and canonical IDs. Document and test necessary compatibility changes.
5. Use existing source-of-truth services/repositories, not direct UI/provider shortcuts.
6. Do not introduce a second memory system. Agents, voice and integrations share central evidence and conversations.
7. Do not introduce a second permissions system. Work within `PermissionService` and current owner/product/workspace semantics.
8. Do not bypass approval/action logging, exact-request validation or outcome capture.
9. External tools must use the existing ToolRegistry/action pipeline and provider boundaries.
10. High-risk actions require explicit permission and approval; mandatory ceilings survive level 5.
11. Run tests/typecheck/build after each implementation milestone, plus configured formatting and realistic end-to-end verification.
12. Do not refactor unrelated code during feature work. Potential dead code is an audit finding, not permission to delete it.
13. Update **this roadmap** after every completed milestone: status, evidence/date, remaining gaps and ordering. Do not create parallel roadmap files.
14. Separate “implemented,” “verified with fixtures,” “verified with real providers” and “deployed.” Passing a mocked Google flow does not mean Google is connected.
15. Keep references as examples/packages behind Ary-owned interfaces. No wholesale Mem0/Graphiti/LangGraph/XYFlow/LiveKit application copy.
16. Distinguish atomic database transactions from external success/local receipt recovery. Preserve uncertain operation guards; never blindly resend or repeat a side effect.
17. Missing information stays unknown. No fabricated memory, financial effect, account balance, provider success or synthetic-data claim in the real workspace.

## DESIGN GUARDRAILS

Ary Nexus should feel like a **spatial operating system**: cinematic, premium, calm, intelligent, holographic, dark, restrained and high-performance. Preserve the current spatial hierarchy, readable glass/holographic surfaces, blue/white/violet lighting, isolated warning orange, smooth spring-based motion, reduced-motion support and graceful fallback.

Reuse `components/spatial/motion.ts`, the shared camera/focus lifecycle, scoped CSS variables and current graph effects before adding a motion library. Use visible states to communicate actual selection, evidence, approval, execution or failure. Number transitions must not obscure exact financial values. Ambient effects must remain subordinate to content; idle navigation settles.

Avoid generic SaaS card grids as the defining experience, cyberpunk clutter, excessive neon, constant idle motion, unreadable transparency, unnecessary 3D and decoration without informational purpose. Existing functional screens must remain usable during visual integration. The new unified hero layout is planned, not retroactively DONE. Native keyboard/HTML alternatives must remain available when Canvas/WebGPU, camera or motion is unavailable.

## SECURITY GUARDRAILS

- Apply least privilege to user/tool/action/product and provider scopes; distinguish service permissions from database owner isolation. Owner RLS alone is not an agent capability policy.
- Desktop Bridge v1 binds authority to the owning loopback launcher, exact request origin/session, explicit flag and pinned authenticated owner. Preserve its fixed structured verbs, mandatory approvals and fail-closed uncertain-operation guards.
- Enforce same-origin mutation rules, authentication and ownership; retain OAuth one-use state/browser binding. Review non-browser/no-Origin behavior before public deployment.
- Accept no caller-supplied shell commands. Prefer `execFile`/argument arrays over `exec`; the existing server launcher uses fixed `spawn` arguments without a shell. Never interpolate user/model text into commands.
- No shutdown/restart/file deletion capability, autonomous money movement/trading or silent high-risk external actions.
- Keep full action/approval/outcome history and source attribution, with documented access/retention. Do not equate “not in semantic memory” with “not stored.”
- Preserve idempotency, optimistic concurrency and rollback/recovery where applicable. Uncertain external outcomes require verification, not an automatic retry that repeats the effect.
- Keys remain in environment/managed secret storage; never hardcode, log or copy them into UI/test artifacts. Local encrypted vault is single-host only; credentials must not enter general repositories or prompts.
- Email/document/provider content is untrusted evidence, never instructions authorizing tools, recipients, approvals or fact supersession.
- Before internet-facing/multi-host operation, add demonstrated usage/abuse limits, retention/restore procedures, managed vault/locking, security-header review and adversarial tenant/permission tests. These are rollout prerequisites, not claims of a discovered exploit.

## TEST GATES

Every future implementation milestone must pass:

1. Existing automated regression tests; preserve their meaning, not merely their count.
2. Meaningful milestone-specific tests, including denial, stale inputs, duplicates and failure/recovery where relevant.
3. `npm run typecheck`.
4. `npm run format:check` and any subsequently configured lint checks. Currently no separate lint command exists.
5. `npm run build`.
6. A realistic end-to-end test of the actual path; label storage, provider, device and API mocks precisely. Use disposable fixtures for destructive/failure cases and exact approval for real external writes.
7. Regression verification of affected existing interfaces/UI, reduced motion and accessible fallbacks where visual behavior changes.
8. Roadmap update with exact files/migrations, fresh results, live deployment state and honest limitations.

Do not mark a milestone DONE unless its scoped gates pass. A successful build or synthetic camera is insufficient proof of Google account access or physical-device quality. Reuse completed recent evidence where scope is unchanged, explicitly date it, and rerun checks when implementation or environment changes invalidate it. Do not invent financial data or send messages to make a test pass.

Current recovery gate records: `/tmp/ary-recovery-tests.log`, `/tmp/ary-recovery-typecheck.log`, `/tmp/ary-recovery-format.log`, `/tmp/ary-recovery-build.log`. Temporary logs support the durable recovery report. Recovery performed the documented minimal reconciliation/audit fixes and paid embedding/extraction calls. It did not perform speech, hardware, or external integration acceptance.

## ACTIVE RISKS / TECHNICAL DEBT

No critical defect was established by this bounded audit; that is not a security certification. Ranked findings:

| Rank                           | Finding / evidence                                                                                                                                                                                                                                              | Required follow-up                                                                                                                                                                                   |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **high**                       | **Background-job lifetime:** original duplicate-link extraction failure is fixed/recovered. Job draining still depends on the existing request/host lifetime.                                                                                                   | Retain failed-job audits and use checked, leased retries after restart. Do not reset attempt limits or delete evidence.                                                                              |
| **high before public rollout** | No general distributed usage limiter, established managed multi-host token vault/lock, or documented end-to-end restore/retention verification found. Existing owner isolation, origin checks and provider bounds are useful but insufficient rollout evidence. | Explicit operational/security gate before exposure; preserve provider/action interfaces. OAuth send guards must survive crashes and key rotation.                                                    |
| **high for voice completion**  | Speech allowlist fixed with explicit approval; real synthetic TTS/STT/Brain passed. Physical/application round trip, first audio, cancel timing and voice-action acceptance remain unverified.                                                                  | Complete physical/app acceptance with the user; measure actual playback/cancel timing and reviewed voice actions. Preserve provider choices and original failed-call evidence.                       |
| **medium**                     | Retrieval graph expansion, entity resolution, Priority, Reflection, Board, ROI and Finance aggregate tenant data on the server. Repository pages transport in chunks of 1,000 but callers still accumulate full histories.                                      | Measure realistic datasets, then add bounded/indexed query/aggregation methods in place. Bounded model input does not mean bounded server work.                                                      |
| **medium**                     | Reflection/extraction restart draining and production review UX are incomplete; Reflection review endpoints intentionally return 404 in production.                                                                                                             | Decide the approved production operator workflow/worker lifecycle; do not simply expose dev endpoints publicly.                                                                                      |
| **medium**                     | Calendar/Gmail successful fixture tests can be mistaken for connected services; OAuth secrets absent, live acceptance pending.                                                                                                                                  | Keep visible disconnected states and IP-4/IP-5 statuses until real checks pass.                                                                                                                      |
| **medium**                     | Financial latest-statement coverage, missing balances, unknown prices and small import bounds limit completeness. Finance SQL enforces core invariants, while full payload semantics also rely on Zod validation.                                               | Real-source acceptance and reconciliation/pagination; no full-net-worth or causal explanation claim beyond supported records.                                                                        |
| **medium**                     | Audit receipts persist bounded email/financial content; confidence/attribution and model-cost estimates are not independent verification/invoices. Audio cost/usage is currently unknown.                                                                       | Define retention/access and cost completeness; never turn null into zero or treat model claims as confirmed value.                                                                                   |
| **medium**                     | Provider/retrieval/graph benchmarks are small historical samples. Contradiction coverage is bounded; Board snapshot/reference validation cannot prove interpretation quality.                                                                                   | Larger labeled relevance/contradiction/board evaluation with missing/ambiguous evidence and no external action authority.                                                                            |
| **medium**                     | Physical hand tracking, microphone quality, touch and assistive-technology behavior are not fully device-validated. Canvas uses HTML alternatives but no screen-reader certification exists.                                                                    | Targeted real-device/accessibility acceptance and false-trigger measurements, preserving reduced-motion/2D fallback.                                                                                 |
| **medium**                     | Desktop is source-live/ad-hoc signed, not a notarized standalone release. Generated `.next/types/* 2.ts` duplicates were observed in earlier runs.                                                                                                              | Investigate the duplication source if it recurs; keep generated-file fixes out of source types. Define packaging/release provenance before distribution.                                             |
| **low**                        | Potential unused legacy `components/entity-graph.tsx` and `brain-graph-panel.tsx` exports: no current consumer found in the inspected source. Multiple graph access methods have distinct legacy/view/retrieval roles.                                          | Confirm consumers/tests before any cleanup; leave them intact in unrelated milestones. Do not add a third parallel graph abstraction.                                                                |
| **low**                        | Deliberate `mock.*` tools remain in the registry/UI, including mock notes/drafts/summaries alongside separately named real task/project tools. Explicit mock/local model providers remain for development.                                                      | Keep simulated labels and namespace separation. These are not evidence that real create/update tools are missing. Hide demos during later productization if needed; do not remove test coverage now. |
| **low**                        | Historical docs contain stale “next” tasks, audio-only desktop policy and Finance→ROI mappings; current screens use both legacy light and newer dark themes.                                                                                                    | This roadmap overrides status/order; current code and later reports override earlier snapshots. Unify visuals only under NEXT 8.                                                                     |

**Biggest risk:** treating the now-passing bounded recovery acceptance as proof of full operational readiness; independent job hosting, restore/retention and live speech/Google/device acceptance remain unverified. **Biggest opportunity:** turn the already-working shared-memory Board/Priority findings into explicitly reviewed existing internal actions, closing the reasoning → evidence → approved work loop without rebuilding the Brain.

## PRODUCT VISION

**Ary = the AI intelligence and voice. Ary Nexus = the operating hub.**

Ary Nexus should become a persistent intelligence operating system that can remember, reason, visualize knowledge, understand projects/people/money/goals, take permissioned actions, coordinate agents, operate through voice and spatial interaction, measure its own value, and connect to Clevaryn, Wag Trails, finance and future products.

Today it has substantial tested intelligence, internal-action and spatial foundations; advisory coordination and source-based financial visibility already exist. Live speech acceptance, Google connections, broader operational readiness and physical interaction quality remain explicit work. Recovery schema parity and isolated memory/Economics acceptance are verified. Seed company/project entities are not integrations, advisory roles are not autonomous workers, and financial visibility is not financial execution.

## Bounded implementation note — Connections Control Center V1 (2026-09-11)

Implemented a read-only owner-facing Connections overview derived from the existing ToolDiscoveryService and adapter evidence. It aggregates connection families conservatively, labels configured versus live evidence, preserves existing domain screens, and does not perform automatic provider tests or change integration state. Verification: typecheck passed; full test and production build run after implementation.

## Overnight realtime voice checkpoints (September 12)

- Input gate: owner supplied 439 captured/forwarded frames, both VAD flags, no errors, CLOSED/cleanup true; owner-reported live input PASS.
- Stage 2F: bounded authenticated output relay and dedicated playback implemented; 1,640 tests, typecheck/build passed; live PCM output verified without speakers. OWNER_PLAYBACK_ACCEPTANCE_PENDING. See [voice checkpoint report](docs/ary-realtime-voice.md). Further overnight stages are explicitly authorized by the current owner prompt; unrelated NEXT 3 is unchanged.
- Stage 2G: response-specific barge-in, duplicate/late-event suppression and browser echo cancellation implemented; 1,641 tests / 100 files, typecheck and build passed. Physical barge-in/echo acceptance pending; external-effect authority unchanged.
- Stage 2H: local wake provider boundary and asset/setup checks implemented; production recognition remains BLOCKED on licensed custom Hey Ary model and inference packaging. Wake remains disabled. Synthetic lifecycle tests only; no owner hardware used. Independent later voice stages may continue under the overnight prompt.
- Stage 2I: local owner-verification contracts, encrypted IndexedDB templates and explicit enrollment UI implemented; synthetic tests passed. SPEAKER_MODEL_SETUP_REQUIRED / OWNER_VOICE_ENROLLMENT_REQUIRED. No owner voiceprint collected; approvals unchanged.
- Stage 2J: opt-in final-transcript → existing AryBrainService → canonical-text Realtime speech implemented. Automatic provider answers disabled; no direct tool authority. Real local Brain memory/message integration and fake-provider cancellation tests passed; live handshake/REAL_RELAY passed. Physical full workflow remains pending.

### Overnight voice Stage 2K checkpoint

- IMPLEMENTED / SYNTHETICALLY VERIFIED: existing activation service coordinates wake
  release, optional local owner gate, authenticated relay capture, canonical Brain,
  playback, barge-in, exact end commands, inactivity and return to local sleep.
- Developer harness: `/mic-test`, including bounded playback metrics and gated Hey Ary
  setup. No automatic microphone start or owner enrollment.
- BLOCKED: real Hey Ary custom model/inference packaging; speaker model/calibration.
  OWNER_ACCEPTANCE_PENDING: audible playback, barge-in/echo, physical wake and enrollment.
- This does not mark the complete Hey Ary product DONE. Classic voice is preserved.

### Overnight voice Stage 2L final status — September 12

- Realtime output, bounded pacing, barge-in, canonical Brain delegation and
  wake/session/sleep composition: IMPLEMENTED / SYNTHETICALLY VERIFIED.
- Live provider handshake, synthetic input relay and paced silent output: PASS.
  Output: 127,200 bytes, first provider audio 582 ms; no physical speaker was used.
- Final validation: 1,677 tests / 105 files PASS; typecheck/build PASS; production
  dev-harness 404 and cross-origin write 403 PASS. Format check retains only the four
  unrelated baseline warnings. Existing DONE milestones preserved.
- BLOCKED: licensed custom Hey Ary model/inference packaging; local speaker
  model/calibration. OWNER_ACCEPTANCE_PENDING: playback/prosody, echo/barge-in,
  enrollment, real Brain/provider voice and full wake cycle. HEY ARY: NOT READY.
- No dependencies added, production deployment, device use, owner enrollment or
  external actions performed. No subsequent unrelated milestone started.
- Canonical current voice status and morning procedure: [Ary realtime voice](docs/ary-realtime-voice.md).
